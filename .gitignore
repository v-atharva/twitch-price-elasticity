# data is rebuilt via make targets; only the curated reference tables are committed
data/*
!data/reference/

# outputs (figures + headline estimate files) are committed for the README

.venv/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/
.ipynb_checkpoints/
.DS_Store

# documentation agent workspace
devdocs/

# leaked payout data — STRICTLY LOCAL, never commit
devdocs/twitch-payouts/
**/twitch-payouts/
*payout*.csv
*payout*.csv.gz
data/external/
